{
  "name": "note",
  "version": "0.0.1",
  "description": "A small editer",
  "main": "main.js",
  "dependencies": {
    "bootstrap": "^3.3.7",
    "jquery": "^3.2.1",
    "lowdb": "^1.0.0",
    "marked": "^0.3.9",
    "node-schedule": "^1.2.5",
    "phantom": "^4.0.12"
  },
  "devDependencies": {
    "electron-packager": "^10.1.0",
    "electron": "^1.7.10",
    "electron-prebuilt": "^1.4.13",
    "electron-rebuild": "^1.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron main.js",
    "packager": "electron-packager . ErtuilEditer --all --out ./OutApp --version 0.0.1 --overwrite --icon=./ertuil.png",
    "packageDarwin": "electron-packager . ErtuilEditer --platform=darwin --arch=x64 --icon=./ertuil.icns --out=./OutApp  --app-version=0.0.3  --overwrite",
    "packageLinux": "electron-packager . ErtuilEditer --platform=linux --arch=x64 --icon=./ertuil.icns --out=./OutApp  --app-version=0.0.3  --overwrite",
    "packageWindow": "electron-packager . ErtuilEditer --platform=win32 --arch=x64 --icon=./ertuil.icns --out=./OutApp  --app-version=0.0.3  --overwrite "
  },
  "author": "Ertuil",
  "license": "ISC"
}
